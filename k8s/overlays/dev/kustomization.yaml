apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: atlas-platform

patches:
  - path: deployment-patch.yaml
  - path: hpa-patch.yaml

commonLabels:
  environment: dev